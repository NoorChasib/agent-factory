# systemd skeleton

The `systemd --user` service, environment contract, credential wiring, graceful shutdown, and
reboot recovery assets belong to Phase 6. No unit is shipped in Phase 1, so installing this
repository cannot start the controller or provision credentials.
